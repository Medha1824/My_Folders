#include <stdio.h>

int main()
{

   int n1, n2,gcd;
      scanf("%d %d" ,&n1, &n2);
      int min=(n1<n2)?n1:n2;
      for(int i=1;i<=min;i++){
         if(n1%i==0 && n2%i==0)
            gcd=i;
      }
      printf("GCD is %d",gcd);


    return 0;
}
