#include<stdio.h>
#include<math.h>

int main()
{
int n,sum=0;
scanf("%d",&n);
for(int i=n-1;i>0;i--){
    if(n%i==0){
        sum+=i;
    }
}
if(sum==n)
    printf("%d is a perfect number",n);
else
    printf("Not");
    return 0;
}
